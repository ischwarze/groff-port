Fix \A and \w regression in groff-1.23 (to be pushed upstream).
Index: src/roff/troff/input.cpp
--- src/roff/troff/input.cpp.orig
+++ src/roff/troff/input.cpp
@@ -1508,9 +1508,9 @@ static node *do_bracket()
 
 static int do_name_test()
 {
-  int start_level = input_stack::get_level();
   token start;
   start.next();
+  int start_level = input_stack::get_level();
   bool got_bad_char = false;
   bool got_some_char = false;
   for (;;) {
@@ -5275,9 +5275,9 @@ static void do_register()
 
 static void do_width()
 {
-  int start_level = input_stack::get_level();
   token start;
   start.next();
+  int start_level = input_stack::get_level();
   environment env(curenv);
   environment *oldenv = curenv;
   curenv = &env;
